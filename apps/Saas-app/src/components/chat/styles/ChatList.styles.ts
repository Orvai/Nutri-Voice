import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: 300,
    backgroundColor: "#fff",
    borderLeftWidth: 1,
    borderLeftColor: "#e5e7eb",
    paddingVertical: 12,
  },
});