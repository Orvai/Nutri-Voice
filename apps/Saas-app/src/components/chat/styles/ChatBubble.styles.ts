import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
  },
  bubble: {
    maxWidth: "75%",
    padding: 12,
    borderRadius: 14,
  },
  messageText: {
    fontSize: 15,
  },
  timeText: {
    marginTop: 6,
    fontSize: 11,
  },
});