import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  tag: {
    borderRadius: 10,
    paddingHorizontal: 10,
    paddingVertical: 6,
  },

  text: {
    fontSize: 12,
    textAlign: "right",
  },
});
